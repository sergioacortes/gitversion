name: CleanArchitecture - PR

env:
  GITVERSION_VERSION: 6.0.3

on:
  pull_request:
    types: [opened, synchronize, reopened]    

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      